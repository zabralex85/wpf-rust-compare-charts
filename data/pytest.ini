[pytest]
# This project has no async tests. The globally-installed pytest-asyncio
# plugin triggers hundreds of DeprecationWarnings on Python 3.14+ during
# collection (asyncio.iscoroutinefunction). Disable it so test output stays
# pristine. Remove this if async tests are ever added.
addopts = -p no:asyncio
