<UserControl x:Class="TelemetryPoc.App.Views.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="46" Background="{StaticResource Panel2}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
            <TextBlock Text="INU&#183;MONITOR" Foreground="{StaticResource AccentCyan}"
                       FontFamily="{StaticResource SansFont}" FontWeight="Bold" FontSize="16" />
            <TextBlock Text="AC 4X-ELT / FLT 1182" Foreground="{StaticResource TextDim}"
                       FontFamily="{StaticResource MonoFont}" FontSize="11"
                       VerticalAlignment="Center" Margin="14,0,0,0" />
        </StackPanel>
        <TextBlock Grid.Column="1" Text="OVERVIEW" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{StaticResource AccentCyan}" FontFamily="{StaticResource SansFont}"
                   FontSize="12" FontWeight="SemiBold" />
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
            <Border Background="{StaticResource AlarmPillBg}" CornerRadius="12" Padding="8,4" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource Red}" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding AlarmText}" Foreground="{StaticResource Red}" FontFamily="{StaticResource SansFont}" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CautionPillBg}" CornerRadius="12" Padding="8,4" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource Amber}" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="{Binding CautionText}" Foreground="{StaticResource Amber}" FontFamily="{StaticResource SansFont}" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
            <TextBlock Text="LINK 1553B&#183;OK" Foreground="{StaticResource Green}"
                       FontFamily="{StaticResource MonoFont}" FontSize="11" Margin="0,0,16,0" />
            <TextBlock x:Name="ClockText" Text="{Binding ClockText}" Foreground="{StaticResource TextData}"
                       FontFamily="{StaticResource MonoFont}" FontSize="13" />
            <StackPanel Orientation="Horizontal" Margin="10,0,0,0"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
                <Button Content="&#9472;" Click="OnMinimize" Style="{StaticResource CaptionButton}" />
                <Button Content="&#9633;" Click="OnMaximize" Style="{StaticResource CaptionButton}" />
                <Button Content="&#10005;" Click="OnClose" Style="{StaticResource CaptionButtonClose}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
